server: 
  port: 8080
spring:
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
    mcp:
      client:
        name: mall-mcp-client
        type: SYNC
        sse:
          connections:
            server1:
              url: http://localhost:8001
              sseEndpoint: /sse
        toolcallback:
          enabled: true
logging:
  level:
    com.wn: debug