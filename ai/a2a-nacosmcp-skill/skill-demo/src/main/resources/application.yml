server:
  port: 8084

spring:
  application:
    name: skill-demo-service
  ai:
    dashscope:
      api-key: ${AI_DASHSCOPE_API_KEY}
      chat:
        options:
          model: qwen-turbo

logging:
  level:
    com.alibaba.cloud.ai: DEBUG